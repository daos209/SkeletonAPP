
export const androidConfig = {
  // Configuración específica para Android
};

export const iosConfig = {
  // Configuración específica para iOS
};

export const mobilewebConfig = {
  // Configuración específica para mobileweb
};